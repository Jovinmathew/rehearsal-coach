# Python
__pycache__/
*.pyc
.venv/
.env
*.egg-info/
.pytest_cache/

# Node
node_modules/
.next/
out/
dist/
npm-debug.log*

# Next.js auto-regenerates these on `next dev`/`next build` (agent-guidance
# stubs pointing at its own docs) — not project documentation, don't commit
frontend/AGENTS.md
frontend/CLAUDE.md

# OS
.DS_Store

# Whisper model cache (downloaded at runtime)
backend/.cache/
